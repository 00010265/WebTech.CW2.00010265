extend layout.pug 

block number1
		.create-blog
				h2 Fill the form to create a blog
				form(action="/create" method="POST")
					if error
						p.error Please add all the information before submitting
					label(for='title') Blog title:
					input#title(type='text' name='title')
					label(for='snippet') Blog snippet:
					input#snippet(type='text' name='snippet')
					label(for='body') Blog body:
					textarea#body(name='mainbody')
					button Submit the blog
